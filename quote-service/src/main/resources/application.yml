spring:
  application:
    name: quote-service
management:
  endpoints:
    web:
      exposure:
        include: "*"  
  endpoint:
    health:
      show-details: always
# logging:
#   level:
#     ROOT: DEBUG
